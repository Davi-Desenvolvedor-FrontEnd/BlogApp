<div class="p-5 mb-4 bg-info-subtle rounded-3">
  <div class="container-fluid d-flex flex-column gap-2">
    <h1 class="display-5 fw-[400] fs-2">Bem-vindo ao BlogApp</h1>
    <p class="col-md-8 fs-5">
      Aqui você pode criar categorias e postagens do jeito que quiser.
    </p>
    {{#unless postagens.length}}
    <hr />
      <a href="/user/registro"><button
          class="btn btn-primary btn-lg align-self-start fs-6"
          type="button"
        >Crie uma conta</button></a>
    {{/unless}}
  </div>
</div>
<div class="container my-4">
  <h2 class="mb-4">Postagens recentes:</h2>
    {{#each postagens}}
      <div class="card mb-4 shadow-sm">
        <div class="card-body">
          <div
            class="d-flex gap-3 justify-content-between align-items-center mb-2"
          >
            <h3 class="card-title">{{titulo}}</h3>
            <small class="bg-primary badge py-2">{{categoria.nome}}</small>
          </div>
          <p class="card-text">{{descricao}}</p>
          <div class="d-flex justify-content-between align-items-center">
            <small class="text-muted">Criado em {{data}}</small>
          </div>
            <a
              href="/postagens/{{slug}}"
              class="btn btn-outline-primary btn-sm mt-2"
            >Leia mais</a>
        </div>
      </div>
    {{else}}
      <p>Nenhuma postagem criada</p>
    {{/each}}
</div>